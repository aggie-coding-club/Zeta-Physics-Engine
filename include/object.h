#ifndef OBJECT_H
#define OBJECT_H

#include "zmath.h"

namespace PhysicsHandler {
    class Object {
        // * add attriutes and necessary functions (position, velocity, etc.)
        // * note: attributes used in the physics update should be public as it speeds up physics computations related to it
    };
}

#endif // ! OBJECT_H
