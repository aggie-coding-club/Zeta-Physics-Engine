#ifndef PHYSICS_HANDLER_H
#define PHYSICS_HANDLER_H

#include "primitives.h"

namespace PhysicsHandler {
    class Handler {
        // * add list of objects (should be pointers to objects but if you're not comfortable 
        // *implementing that, I can do that part after you implement it with non-pointers)

        // * add functions to act on the list of objects

        // * add update functionality using kinematic equations
    };
}

#endif // ! PHYSICS_HANDLER_H
